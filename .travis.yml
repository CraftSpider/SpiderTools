sudo: false

language: python

cache: pip

matrix:
  include:
    - name: "Python 3.7"
      python: 3.7
      dist: xenial
      sudo: true

services:
  - mysql
  - postgresql

install:
  - pip install "." .

script: python3 -m pytest tests
